/*
graph as adjacency list 
	List<Integer>[] nodes	

arrays
	dist
	from
	visited

node queue with src
while queue is not empty
	pop node
	navigate all neighbor nodes 
		if(neighbor is not visited)
			mark dist and node+1 
			mark from to node (tree)
			queue 
	mark node as visited

visualize?
 */
public class BFS {
	public void search() {

	}
}